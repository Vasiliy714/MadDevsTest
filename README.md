 Cкрипт Python для разделения сообщений HTML на фрагменты указанной длины с сохранением структуры HTML.

## Использование

1. Установка зависимостей:
   ```bash
   pip install -r requirements.txt

2. Запуск скрипта:
   ```bash
   python main.py <max_len> <html_file>